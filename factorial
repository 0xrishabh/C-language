#include <stdio.h>
#include <stdlib.h>

int main(){
    int num;
    int fact=1;
    printf("ENTER ANY NUMBER \n");
    scanf("%d",&num);
    if (num < 2){printf(1);}
    else{
        for(int i=2;i<=num;i++){
            fact = i*fact;
        }
        printf("%d",fact);
    }
}
